# Copia in .env.local per puntare `npm run dev` a un backend diverso da localhost:8000.
#
# Backend sul server (dati veri, crediti veri):
# VITE_API_TARGET=https://dietai.spassocasa.it
#
# Backend in locale (default se la variabile non c'è):
# VITE_API_TARGET=http://localhost:8000
